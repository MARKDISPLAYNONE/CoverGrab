# CoverGrab Environment Variables
# Copy this file to .env and fill in the values

# ============================================
# Supabase Configuration
# ============================================
# Get these from your Supabase project settings

# Project URL (e.g., https://xxxxx.supabase.co)
SUPABASE_URL=https://your-project.supabase.co

# Service Role Key (NOT the anon key - use the service_role key for server-side)
# Found in: Project Settings > API > service_role key
SUPABASE_SERVICE_KEY=your-service-role-key-here

# ============================================
# Security
# ============================================

# Salt for hashing IP addresses (use a random string)
# Generate with: openssl rand -hex 32
IP_HASH_SALT=your-random-salt-here

# ============================================
# Admin Authentication (Phase 3)
# ============================================

# Admin email address for login
ADMIN_EMAIL=admin@example.com

# Bcrypt hash of admin password
# Generate with Node.js: 
#   const bcrypt = require('bcryptjs');
#   console.log(bcrypt.hashSync('your-strong-password', 12));
# Or use: https://bcrypt-generator.com/
ADMIN_PASSWORD_HASH=$2a$12$your-bcrypt-hash-here

# Secret for signing JWT tokens (use a long random string)
# Generate with: openssl rand -hex 64
JWT_SECRET=your-jwt-secret-here-at-least-32-characters-long

# (Optional) TOTP secret for 2FA (base32 encoded)
# Generate with an authenticator app or: 
#   npm install otplib && node -e "console.log(require('otplib').authenticator.generateSecret())"
# TOTP_SECRET=JBSWY3DPEHPK3PXP
